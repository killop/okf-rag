[package]
name = "zvec-sys"
version = "0.5.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/zvec-ai/zvec-rust"
rust-version = "1.70"
description = "Raw FFI bindings to the zvec C-API"
readme = "README.md"
links = "zvec_c_api"
keywords = ["ffi", "vector-database", "zvec", "bindings"]
categories = ["external-ffi-bindings", "database"]

[build-dependencies]
